#import "../../../utils.typ": *
= Data-Driven Parameter Discovery (The Inverse Problem)

This chapter addresses the inverse problem in biomedical modeling: identifying unknown physiological parameters (such as insulin sensitivity $S_I$ and epinephrine impairment $rho$) from sparse, noisy clinical data using PINNs.

#include "parameter_discovery.typ"
